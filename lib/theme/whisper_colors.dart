import 'package:flutter/material.dart';

class WhisperColors {
  WhisperColors._();

  // Ana renkler — washi kağıdı, sis, kuru çay tonu
  static const Color background = Color(0xFFF7F4F0);
  static const Color surface = Color(0xFFFDFBF8);
  static const Color ink = Color(0xFF2C2A27);
  static const Color inkLight = Color(0xFF6B6760);
  static const Color inkFaint = Color(0xFFB0ADA8);

  // Vurgu — soluk lavanta, japon mürekkebi
  static const Color accent = Color(0xFF8C7F9E);
  static const Color accentSoft = Color(0xFFD4CCDF);

  // Yüzeyler
  static const Color cardBackground = Color(0xFFF0ECE6);
  static const Color divider = Color(0xFFE5E1DB);

  // Durum renkleri — agresif değil
  static const Color success = Color(0xFF7A9E7E);
  static const Color warning = Color(0xFFBFA980);
}